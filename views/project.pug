extends layout
block headtitle
  h2 Project
block content
  .wrapper
    main.container
      nav.pub_sub_menu
        ul
          li.on(href="#") 국책과제
          li 산학과제
          li 내부과제
      
      section.content
        table.p_content
          thead
            tr
              th Korean Title
              th English Title
              th Date
              th
          tbody
            each pro in project
              -if (pro.date_start != null)
                - if (pro.category == 'national')
                  tr.pub_tr1
                    td= pro.kr
                    td= pro.eng
                    td= `${pro.date_start.getYear()+1900}.${pro.date_start.getMonth()+1} ~ ${pro.date_end.getYear()+1900}.${pro.date_end.getMonth()+1}`
                    td
                      if currentUser 
                        a(href=`/project/delete/${pro._id}`) X              
                - if (pro.category == 'industrial')
                  tr.pub_tr2
                    td= pro.kr
                    td= pro.eng
                    td= `${pro.date_start.getYear()+1900}.${pro.date_start.getMonth()+1} ~ ${pro.date_end.getYear()+1900}.${pro.date_end.getMonth()+1}`     
                    td
                      if currentUser 
                        a(href=`/project/delete/${pro._id}`) X             
                - if (pro.category == 'internal')
                  tr.pub_tr3
                    td= pro.kr
                    td= pro.eng
                    td= `${pro.date_start.getYear()+1900}.${pro.date_start.getMonth()+1} ~ ${pro.date_end.getYear()+1900}.${pro.date_end.getMonth()+1}`     
                    td 
                      if currentUser
                        a(href=`/project/delete/${pro._id}`) X             
                //- td
                //-       a(href=`/project/delete/${pro._id}`)

    if currentUser
      section.btn_box.admin
        button.btn(onclick="location.href='/project/update'") 과제 업로드